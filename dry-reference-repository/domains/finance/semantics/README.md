# Finance semantics (semantic contracts)

Put **semantic contracts** here:
- semantic models (entities + grain + relationships)
- metrics (KPI definitions)

Principles:
- semantic contracts are the primary mechanism for preventing metric drift
- shared/certified semantic contracts should be versioned and registered

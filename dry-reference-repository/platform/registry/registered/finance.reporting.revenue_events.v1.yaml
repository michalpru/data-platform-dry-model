# Registered by CI on merge to main.
# Promoted from: domains/finance/contracts/datasets/finance.reporting.revenue_events.v1.contract.yaml
#
# Registry scope: certified artifacts intended for reuse beyond the owning team.
# Domain-local artifacts (lifecycle: local) are NOT registered here.
# Ownership: Finance Analytics team — they own the definition; the registry records it.
apiVersion: dry.platform/v1
kind: DryArtifact
metadata:
  fqn: finance.reporting.revenue_events.v1
  title: Revenue Events
  description: Certified finance dataset for reporting. Canonical source for revenue event facts.
  owner:
    team: finance-analytics
    escalation: finance-data-oncall
  lifecycle:
    state: certified
spec:
  interfaceTypes:
    - queryable_dataset
  physicalImplementations:
    - system: warehouse
      env: prod
      ref: analytics.finance.revenue_events
    - system: warehouse
      env: uat
      ref: analytics_uat.finance.revenue_events
  compatibility:
    policy: backward-compatible
  sourceManifest: domains/finance/contracts/datasets/finance.reporting.revenue_events.v1.contract.yaml
